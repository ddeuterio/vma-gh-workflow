name: Semgrep CE scan

on:
  # Standalone triggers (when deployed to individual repos)
  pull_request:
  workflow_dispatch: {}
  push:
    branches: ["main"]

  # Reusable workflow trigger (called from ci.yml)
  workflow_call:
    inputs:
      ref:
        required: false
        type: string
        default: ''
      fail_on_error:
        description: 'Fail the build on HIGH/CRITICAL severity findings'
        required: false
        type: boolean
        default: false

  # Schedule the CI job (this method uses cron syntax):
  # schedule:
  #   - cron: '20 17 * * *' # Change to a random time

permissions:
  contents: read
  security-events: write  # Required for SARIF upload

jobs:
  semgrep:
    name: semgrep-oss/scan
    runs-on: ubuntu-latest # docker is not installed in slim
    container:
      image: semgrep/semgrep

    if: (github.actor != 'dependabot[bot]')

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref || github.ref }}
          # fetch-depth: 0  # Full git history for diff scanning

      - name: Run Semgrep CI
        env:
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}
        run: |
          # Verify token is set
          if [ -z "$SEMGREP_APP_TOKEN" ]; then
            echo "Error: SEMGREP_APP_TOKEN is not set"
            exit 1
          fi

          # Run semgrep ci with SARIF output
          semgrep ci --sarif-output=semgrep.sarif
        continue-on-error: ${{ inputs.fail_on_error == false }}

      - name: Upload SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()  # Upload even if scan found issues
        with:
          sarif_file: semgrep.sarif
